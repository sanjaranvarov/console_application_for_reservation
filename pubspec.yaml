name: console_application_for_reservation
description: A sample command-line application.
version: 1.0.0


environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
# path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
